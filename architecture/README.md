# Архитектура: Альфа-будущее

Фича / демо: mobile-first веб «Альфа · Инвестиции».

## ЦА (пока так)

**18–24 года**, нет инвестиционного счёта или счёт пустой.

## Код

- `web/` — UI (Vite React)
- `server/` — FastAPI
  - `stubs/alfa.py` — профиль клиента
  - `stubs/market.py` — рынок
  - `stubs/portfolio.py` — paper-портфель
  - `ollama_client.py` — агент `bonsai-27b`

## Поток

```
онбординг маскота → портфель / каталог / сделка / пульс / учёба / агент
```

ИИ только объясняет и чатит. Выбор продукта — правила + UI, не модель.

Маскот: `hello` речь, `buy` покупка/прибыль, `type` ввод.
